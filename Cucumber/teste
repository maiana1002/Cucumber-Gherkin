const { Given, When, Then } = require('@cucumber/cucumber');
const assert = require('chai').assert;

Given('que criei o arquivo corretamente', function () {
  // Lógica para o Given
});

When('executá-lo', function () {
  // Lógica para o When
});

Then('a especificação deve finalizar com sucesso', function () {
  // Lógica para o Then
  assert(true); // Exemplo de validação
});
